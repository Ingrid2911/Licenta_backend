<Window x:Class="time_expanded_graph.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:time_expanded_graph.View.Controls"
        Title="EvacPath"
        Height="900"
        Width="1000">

    <Grid Background="#181818">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="250"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="110"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#202529"
                    Padding="8">
                <StackPanel VerticalAlignment="Center">

                    <TextBlock FontSize="22"
                               FontWeight="Bold"
                               Text="{Binding ResultText}"
                               Foreground="White"
                               TextWrapping="Wrap"/>

                    <TextBlock Margin="0,6,0,0"
                               FontSize="16"
                               Text="{Binding FlowResultsText}"
                               Foreground="White"
                               TextWrapping="Wrap"/>

                </StackPanel>
            </Border>

            <controls:GraphTabsView x:Name="GraphTabs"
                                    Grid.Row="1"/>
        </Grid>

        <controls:ParametersPanel Grid.Column="1"/>
    </Grid>
</Window>